# Infra Rotation Challenge
Start this simple Go lang application with Docker by running the following command on your terminal:

`$ make run`

Or by creating a Kubernetes cluster with the following commands:

```
$ make minikube
$ make cluster
```
